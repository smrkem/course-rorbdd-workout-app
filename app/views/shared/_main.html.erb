<div class="container">
  <div class="col-md-12">
    <% flash.each do |key, msg| %>
      <div class="text-center alert alert-<%= key == 'notice' ? 'success' : 'danger' %>">
        <%= msg %>
      </div>
    <% end %>
  </div>
  <%= yield %>
</div>
